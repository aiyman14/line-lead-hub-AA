name: Desktop Build

on:
  workflow_dispatch:
  push:
    tags:
      - "desktop-v*"

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            platform: linux-x86_64
          - os: windows-latest
            platform: windows-x86_64
          - os: macos-latest
            platform: darwin-universal

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-22.04'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            librsvg2-dev \
            libsoup-3.0-dev \
            patchelf
          sudo apt-get install -y libayatana-appindicator3-dev || sudo apt-get install -y libappindicator3-dev

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Add macOS universal targets
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          rustup target add x86_64-apple-darwin
          rustup target add aarch64-apple-darwin

      - name: Install frontend deps
        shell: bash
        run: npm ci

      - name: Write Tauri updater signing key
        shell: bash
        run: |
          mkdir -p ~/.tauri
          printf '%s' "${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}" > ~/.tauri/tauri.key
          chmod 600 ~/.tauri/tauri.key

      - name: Import Apple Developer ID certificate
        if: matrix.os == 'macos-latest'
        uses: apple-actions/import-codesign-certs@v3
        with:
          p12-file-base64: ${{ secrets.APPLE_CERTIFICATE }}
          p12-password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}

      - name: Build desktop app (Linux/Windows)
        if: matrix.os != 'macos-latest'
        shell: bash
        run: npm run tauri:build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Build desktop app (macOS Universal)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: npm run tauri:build -- --target universal-apple-darwin
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Codesign app properly + rebuild DMG (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        env:
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        run: |
          set -euo pipefail

          mkdir -p artifacts

          APP_PATH="$(find "src-tauri/target/universal-apple-darwin/release/bundle/macos" -maxdepth 2 -name "*.app" -type d | head -1 || true)"
          if [ -z "${APP_PATH:-}" ]; then
            APP_PATH="$(find "src-tauri/target/release/bundle/macos" -maxdepth 2 -name "*.app" -type d | head -1 || true)"
          fi

          if [ -z "${APP_PATH:-}" ] || [ ! -d "$APP_PATH" ]; then
            echo "Could not find .app to sign" >&2
            exit 1
          fi

          echo "App found: $APP_PATH"
          echo "Signing identity: $APPLE_SIGNING_IDENTITY"

          ENTITLEMENTS="src-tauri/entitlements.plist"
          if [ ! -f "$ENTITLEMENTS" ]; then
            echo "WARNING: $ENTITLEMENTS not found. Signing without entitlements."
            ENTITLEMENTS=""
          fi

          echo "Signing nested items..."
          while IFS= read -r item; do
            if [ -n "${ENTITLEMENTS:-}" ]; then
              codesign --force --options runtime --timestamp \
                --sign "$APPLE_SIGNING_IDENTITY" \
                --entitlements "$ENTITLEMENTS" \
                "$item"
            else
              codesign --force --options runtime --timestamp \
                --sign "$APPLE_SIGNING_IDENTITY" \
                "$item"
            fi
          done < <(find "$APP_PATH" -type f \( -name "*.dylib" -o -name "*.so" -o -perm -111 \) -print || true)

          echo "Signing the app bundle..."
          if [ -n "${ENTITLEMENTS:-}" ]; then
            codesign --force --deep --options runtime --timestamp \
              --sign "$APPLE_SIGNING_IDENTITY" \
              --entitlements "$ENTITLEMENTS" \
              "$APP_PATH"
          else
            codesign --force --deep --options runtime --timestamp \
              --sign "$APPLE_SIGNING_IDENTITY" \
              "$APP_PATH"
          fi

          echo "Verifying codesign..."
          codesign --verify --deep --strict --verbose=2 "$APP_PATH"
          spctl -a -vv "$APP_PATH" || true

          STAGE_DIR="$(pwd)/artifacts/dmg_stage"
          rm -rf "$STAGE_DIR"
          mkdir -p "$STAGE_DIR"
          cp -R "$APP_PATH" "$STAGE_DIR/"

          DMG_OUT="artifacts/Production_Portal_universal.dmg"
          rm -f "$DMG_OUT"

          APP_NAME="$(basename "$APP_PATH")"

          if ! command -v create-dmg >/dev/null 2>&1; then
            brew update
            brew install create-dmg
          fi
          if ! command -v magick >/dev/null 2>&1; then
            brew update
            brew install imagemagick
          fi

          BG_W=660
          BG_H=500
          BACKGROUND_RENDERED="$STAGE_DIR/dmg-background-rendered.png"

          # Use a font path that exists on macOS runners (Helvetica is not available to ImageMagick)
          FONT_PATH="/System/Library/Fonts/Supplemental/Arial.ttf"
          if [ ! -f "$FONT_PATH" ]; then
            FONT_PATH="/System/Library/Fonts/SFNS.ttf"
          fi

          magick -size ${BG_W}x${BG_H} gradient:'#202124-#0b0c0d' \
            -attenuate 0.12 +noise Gaussian \
            -stroke 'rgba(255,255,255,0.18)' -strokewidth 2 -fill none \
            -draw "roundrectangle 96,236 264,404 18,18" \
            -draw "roundrectangle 396,236 564,404 18,18" \
            -gravity North -font "$FONT_PATH" -pointsize 34 \
            -fill 'rgba(255,255,255,0.92)' \
            -annotate +0+110 "Drag to Applications →" \
            "$BACKGROUND_RENDERED"

          create-dmg \
            --volname "ProductionPortal" \
            --window-pos 200 120 \
            --window-size ${BG_W} ${BG_H} \
            --icon-size 128 \
            --background "$BACKGROUND_RENDERED" \
            --icon "$APP_NAME" 180 320 \
            --app-drop-link 480 320 \
            "$DMG_OUT" \
            "$STAGE_DIR"

          echo "DMG created:"
          ls -lh "$DMG_OUT"

      - name: Notarize app + rebuild updater bundle (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          # Find the signed .app
          APP_PATH="$(find "src-tauri/target/universal-apple-darwin/release/bundle/macos" -maxdepth 2 -name "*.app" -type d | head -1 || true)"
          if [ -z "${APP_PATH:-}" ]; then
            APP_PATH="$(find "src-tauri/target/release/bundle/macos" -maxdepth 2 -name "*.app" -type d | head -1 || true)"
          fi

          if [ -z "${APP_PATH:-}" ] || [ ! -d "$APP_PATH" ]; then
            echo "Could not find .app to notarize" >&2
            exit 1
          fi

          echo "Notarizing app: $APP_PATH"

          # Create zip for notarization submission (Apple requires zip/dmg/pkg)
          APP_ZIP="$(pwd)/artifacts/app_notarize.zip"
          ditto -c -k --keepParent "$APP_PATH" "$APP_ZIP"

          retry() {
            local n=0 max=6 delay=20
            until "$@"; do
              n=$((n+1))
              if [ "$n" -ge "$max" ]; then
                echo "Command failed after $n attempts: $*" >&2
                return 1
              fi
              echo "Command failed (attempt $n/$max). Retrying in ${delay}s..." >&2
              sleep "$delay"
              delay=$((delay*2))
            done
          }

          echo "Submitting .app for notarization..."
          SUBMIT_JSON="$(retry xcrun notarytool submit "$APP_ZIP" \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_SPECIFIC_PASSWORD" \
            --team-id "$APPLE_TEAM_ID" \
            --wait \
            --output-format json)"

          echo "$SUBMIT_JSON"

          UUID="$(python3 -c 'import json,sys; print(json.loads(sys.argv[1]).get("id",""))' "$SUBMIT_JSON")"
          STATUS="$(python3 -c 'import json,sys; print(json.loads(sys.argv[1]).get("status",""))' "$SUBMIT_JSON")"

          echo "Notary submission id: $UUID"
          echo "Notary status: $STATUS"

          if [ "$STATUS" != "Accepted" ]; then
            echo "App notarization not accepted. Fetching log..." >&2
            xcrun notarytool log "$UUID" \
              --apple-id "$APPLE_ID" \
              --password "$APPLE_APP_SPECIFIC_PASSWORD" \
              --team-id "$APPLE_TEAM_ID" || true
            exit 1
          fi

          echo "Waiting for ticket propagation..."
          sleep 120

          echo "Stapling notarization ticket to .app..."
          retry xcrun stapler staple "$APP_PATH"

          echo "Verifying stapled app..."
          xcrun stapler validate "$APP_PATH" || true
          spctl -a -vv "$APP_PATH" || true

          # Rebuild .app.tar.gz from the signed + notarized app
          APP_DIR="$(dirname "$APP_PATH")"
          APP_BASENAME="$(basename "$APP_PATH")"
          TAR_PATH="$(pwd)/artifacts/Production.Portal.app.tar.gz"

          echo "Rebuilding updater .app.tar.gz from signed+notarized app..."
          tar -czf "$TAR_PATH" -C "$APP_DIR" "$APP_BASENAME"

          # Re-sign with Tauri updater key so auto-updater signature verification still works
          echo "Re-signing updater bundle with Tauri key..."
          npx tauri signer sign "$TAR_PATH" \
            --private-key "$TAURI_SIGNING_PRIVATE_KEY" \
            --password "${TAURI_SIGNING_PRIVATE_KEY_PASSWORD:-}"

          echo "Updater bundle rebuilt and signed:"
          ls -lh "$TAR_PATH" "$TAR_PATH.sig"

          # Clean up temporary zip
          rm -f "$APP_ZIP"

      - name: Notarize + staple DMG (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail

          DMG="artifacts/Production_Portal_universal.dmg"
          if [ ! -f "$DMG" ]; then
            echo "Missing DMG at $DMG" >&2
            exit 1
          fi

          retry() {
            local n=0 max=6 delay=20
            until "$@"; do
              n=$((n+1))
              if [ "$n" -ge "$max" ]; then
                echo "Command failed after $n attempts: $*" >&2
                return 1
              fi
              echo "Command failed (attempt $n/$max). Retrying in ${delay}s..." >&2
              sleep "$delay"
              delay=$((delay*2))
            done
          }

          echo "Submitting for notarization (wait): $DMG"
          SUBMIT_JSON="$(retry xcrun notarytool submit "$DMG" \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_SPECIFIC_PASSWORD" \
            --team-id "$APPLE_TEAM_ID" \
            --wait \
            --output-format json)"

          echo "$SUBMIT_JSON"

          UUID="$(python3 -c 'import json,sys; print(json.loads(sys.argv[1]).get("id",""))' "$SUBMIT_JSON")"
          STATUS="$(python3 -c 'import json,sys; print(json.loads(sys.argv[1]).get("status",""))' "$SUBMIT_JSON")"

          echo "Notary submission id: $UUID"
          echo "Notary status: $STATUS"

          if [ -z "${UUID:-}" ]; then
            echo "ERROR: Could not extract notary submission id." >&2
            exit 1
          fi

          if [ "$STATUS" != "Accepted" ]; then
            echo "Notarization not accepted. Fetching log..." >&2
            xcrun notarytool log "$UUID" \
              --apple-id "$APPLE_ID" \
              --password "$APPLE_APP_SPECIFIC_PASSWORD" \
              --team-id "$APPLE_TEAM_ID" || true
            exit 1
          fi

          echo "Waiting for ticket propagation..."
          sleep 120

          echo "Stapling notarization ticket..."
          retry xcrun stapler staple "$DMG"

          echo "Stapler validate..."
          xcrun stapler validate "$DMG" || true

          echo "Gatekeeper verification..."
          spctl -a -vv "$DMG" || true

      - name: Collect artifacts (Linux)
        if: matrix.os == 'ubuntu-22.04'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts

          APPIMAGE="$(find src-tauri/target/release/bundle -name "*.AppImage" -type f | head -1 || true)"
          if [ -n "${APPIMAGE:-}" ] && [ -f "$APPIMAGE" ]; then
            cp "$APPIMAGE" artifacts/
          fi

          if [ -f "src-tauri/target/release/bundle/appimage/linux-x86_64.AppImage.sig" ]; then
            cp "src-tauri/target/release/bundle/appimage/linux-x86_64.AppImage.sig" artifacts/linux-x86_64.sig
          elif [ -n "${APPIMAGE:-}" ] && [ -f "$APPIMAGE.sig" ]; then
            cp "$APPIMAGE.sig" artifacts/linux-x86_64.sig
          elif [ -f "src-tauri/target/release/bundle/appimage/linux-x86_64.sig" ]; then
            cp "src-tauri/target/release/bundle/appimage/linux-x86_64.sig" artifacts/linux-x86_64.sig
          fi

          find src-tauri/target/release/bundle -name "*.deb" -type f -exec cp {} artifacts/ \; || true
          find src-tauri/target/release/bundle -name "*.rpm" -type f -exec cp {} artifacts/ \; || true

      - name: Collect artifacts (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts

          MSI="$(find src-tauri/target/release/bundle -name "*.msi" -type f | head -1 || true)"
          if [ -n "${MSI:-}" ] && [ -f "$MSI" ]; then
            cp "$MSI" artifacts/
          fi

          if [ -f "src-tauri/target/release/bundle/msi/windows-x86_64.msi.sig" ]; then
            cp "src-tauri/target/release/bundle/msi/windows-x86_64.msi.sig" artifacts/windows-x86_64.sig
          elif [ -n "${MSI:-}" ] && [ -f "$MSI.sig" ]; then
            cp "$MSI.sig" artifacts/windows-x86_64.sig
          elif [ -f "src-tauri/target/release/bundle/msi/windows-x86_64.sig" ]; then
            cp "src-tauri/target/release/bundle/msi/windows-x86_64.sig" artifacts/windows-x86_64.sig
          fi

          find src-tauri/target/release/bundle -name "*.exe" -type f -exec cp {} artifacts/ \; || true

      - name: Collect artifacts (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts

          [ -f artifacts/Production_Portal_universal.dmg ] || (echo "Missing notarized DMG" >&2 && exit 1)

          # The updater tar.gz + sig are rebuilt by the "Notarize app + rebuild updater bundle" step
          [ -f artifacts/Production.Portal.app.tar.gz ] || (echo "Missing rebuilt updater bundle" >&2 && exit 1)

          if [ -f "artifacts/Production.Portal.app.tar.gz.sig" ]; then
            cp "artifacts/Production.Portal.app.tar.gz.sig" "artifacts/darwin-universal.sig"
          else
            echo "WARNING: No .sig found for rebuilt updater bundle" >&2
            exit 1
          fi

      - name: Upload installers
        uses: actions/upload-artifact@v4
        with:
          name: installers-${{ matrix.platform }}
          if-no-files-found: error
          path: artifacts/

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/desktop-v')

    steps:
      - name: Download all installers
        uses: actions/download-artifact@v4
        with:
          pattern: installers-*
          path: dist
          merge-multiple: true
     
      - name: Normalize macOS updater filename
        shell: bash
        run: |
          set -euo pipefail

          MACOS_FILE="$(ls -1 dist/*.app.tar.gz 2>/dev/null | head -1 || true)"
          if [ -z "${MACOS_FILE:-}" ]; then
            echo "No .app.tar.gz found in dist" >&2
            ls -la dist >&2
            exit 1
          fi

          DEST="dist/Production.Portal.app.tar.gz"

          # If it’s already the correct name, do nothing (prevents deleting it)
          if [ "$MACOS_FILE" != "$DEST" ]; then
            cp -f "$MACOS_FILE" "$DEST"
            rm -f "$MACOS_FILE" || true
          fi

          ls -la dist

      - name: Debug dist contents
        shell: bash
        run: |
          set -euo pipefail
          echo "DIST FILES:"
          ls -la dist

      - name: Generate latest.json (from filenames in dist)
        shell: bash
        run: |
          set -euo pipefail

          TAG="${{ github.ref_name }}"
          VERSION="${TAG#desktop-v}"
          REPO="${{ github.repository }}"
          PUB_DATE="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"

          LINUX_FILE="$(ls -1 dist/*.AppImage 2>/dev/null | head -1 || true)"
          WINDOWS_FILE="$(ls -1 dist/*.msi 2>/dev/null | head -1 || true)"
          MACOS_FILE="dist/Production.Portal.app.tar.gz"

          if [ -z "${LINUX_FILE:-}" ] || [ -z "${WINDOWS_FILE:-}" ] || [ -z "${MACOS_FILE:-}" ]; then
            echo "Missing one or more required assets in dist/" >&2
            ls -la dist >&2
            exit 1
          fi

          urlencode() {
            python3 -c 'import urllib.parse,sys; print(urllib.parse.quote(sys.argv[1]))' "$1"
          }

          LINUX_NAME="$(basename "$LINUX_FILE")"
          WINDOWS_NAME="$(basename "$WINDOWS_FILE")"
          MACOS_NAME="$(basename "$MACOS_FILE")"

          LINUX_URL="https://github.com/$REPO/releases/download/$TAG/$(urlencode "$LINUX_NAME")"
          WINDOWS_URL="https://github.com/$REPO/releases/download/$TAG/$(urlencode "$WINDOWS_NAME")"
          MACOS_URL="https://github.com/$REPO/releases/download/$TAG/$(urlencode "$MACOS_NAME")"

          LINUX_SIG="$(tr -d '\n' < dist/linux-x86_64.sig)"
          WINDOWS_SIG="$(tr -d '\n' < dist/windows-x86_64.sig)"
          DARWIN_SIG="$(tr -d '\n' < dist/darwin-universal.sig)"

          cat > dist/latest.json <<EOF
          {
            "version": "$VERSION",
            "notes": "ProductionPortal update",
            "pub_date": "$PUB_DATE",
            "platforms": {
              "linux-x86_64": { "url": "$LINUX_URL", "signature": "$LINUX_SIG" },
              "windows-x86_64": { "url": "$WINDOWS_URL", "signature": "$WINDOWS_SIG" },
              "darwin-aarch64": { "url": "$MACOS_URL", "signature": "$DARWIN_SIG" },
              "darwin-x86_64": { "url": "$MACOS_URL", "signature": "$DARWIN_SIG" }
            }
          }
          EOF

          python3 -m json.tool dist/latest.json >/dev/null
          echo "latest.json generated OK"
          cat dist/latest.json

      - name: Create GitHub release + upload assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*
